{% extends 'base.html.twig' %}

{% block title %}Créer un enfant{% endblock %}

{% block body %}
    <h1>Créer un enfant</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('enfant_index') }}">Retour</a>
{% endblock %}
